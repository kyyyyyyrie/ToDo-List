# ToDo List

An Android ToDo List application with Python Flask backend.

## Project Structure

- `AndroidToDo/` - Android application source code
- `backend/` - Python Flask backend API

## Backend Setup

```bash
cd backend
pip install -r requirements.txt
python app.py
```

## Android Setup

Open `AndroidToDo/` in Android Studio and run the application.